/**
 * Marker interface defined that it's deliverable between any states.
 * @see {State}
 * @interface
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface Deliverable {
  // do nothing
}

export default Deliverable;
